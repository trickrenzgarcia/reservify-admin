export type Test = {
  id: string;
  projectName: string;
};

export type User = {
  id: string;
  email: string;
  name: string;
};

export type AdminUser = {
  id: string;
  email: string;
  name: string;
};

export type AdminLogin = {
  email: string;
  password: string;
};
